#!/usr/bin/env node

var dir = process.argv[2];
if (!dir) {
    console.log('Error: source dirictories is required!');
    process.exit(1);
}

require('./../lib/index.js').scan(dir, function (objects) {
    console.log(JSON.stringify(objects, null, '  '));
});